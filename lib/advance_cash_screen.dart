import 'package:flutter/material.dart';

class AdvanceCashScreen extends StatelessWidget {
  const AdvanceCashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is advance cash screen'),
    );
  }
}
